{
  "name": "rss-list",
  "private": false,
  "version": "0.1.5",
  "main": "dist/rss-list.es.js",
  "exports": {
    ".": "./dist/rss-list.es.js"
  },
  "types": "types/rss-list.d.ts",
  "files": [
    "dist",
    "types"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "release:patch": "npm version patch --force",
    "release:minor": "npm version minor --force",
    "release:major": "npm version major --force",
    "deploy": "npm run release:patch && npm run build && npm publish"
  },
  "dependencies": {
    "lit": "^2.2.3"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.7"
  }
}
